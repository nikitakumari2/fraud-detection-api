# Serving dependencies (what the Docker image installs).
# Versions must match the ones you train with, or the saved model may not load.
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
xgboost-cpu==3.4.1
joblib==1.5.3
fastapi==0.141.1
uvicorn==0.53.0
